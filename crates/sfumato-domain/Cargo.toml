[package]
name = "sfumato-domain"
version = "0.2.0"
edition = "2024"
description = "Pure domain types and review rules for Sfumato"
license = "MIT"

[dependencies]
json-patch = "4.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
