Create a Marp Markdown slide deck.

Project: {{ project }}
Allowed filesystem root: {{ project_root }}
Theme: {{ theme_name }}
Theme colors: {{ theme_colors }}
Theme fonts: {{ theme_fonts }}
Instruction: {{ instruction }}

{% include "shared/project-instructions.md.j2" %}

Requirements:
{% if template_enabled %}
- A reusable structural template named `{{ template_name }}` is active. Return only the Markdown content that replaces its single `<!-- SFUMATO_CONTENT -->` marker.
- Do not repeat the template, its frontmatter, or its static content. Sfumato performs the merge after generation.
{% if title_provided %}- Use exactly "{{ title }}" as the first generated `# H1` when the template does not already provide the title.{% else %}- Create a concise subject title as the first generated `# H1` when the template does not already provide one.{% endif %}
- Your fragment may contain slide separators and must provide the requested teaching content.

Template source (read-only structure):

```markdown
{{ template_source }}
```
{% else %}
{% if title_provided %}
- Use exactly "{{ title }}" as the deck title and as the first `# H1` on the title slide.
{% else %}
- Create a concise, specific deck title that describes the subject being taught. Do not copy or lightly rephrase the instruction as the title. Put your chosen title in the first `# H1` on the title slide; Sfumato will use it as the artifact filename.
{% endif %}
- Return only Markdown.
- Return raw Marp Markdown. Never wrap the deck in a `marp`, `markdown`, or `md` code fence.
- Start the document with Marp frontmatter as the first bytes. Do not put a slide, title, blank `---`, or any text before it.
{% endif %}
- Do not include `style:`, inline CSS, or generated theme CSS in Marp frontmatter.
- Set Marp math rendering to MathJax with `math: mathjax`.
{% include "shared/marp-math-contract.md.j2" %}
- Use slide separators with ---.
- Include a title slide.
- Use `<!-- _class: lead -->` for title and section-divider slides.
- Use `<!-- _class: compact -->` only for dense tables, formulas, or comparison slides.
- Prefer `##` headings for normal content slides after the title slide.
- You may use Mermaid diagrams in fenced ```mermaid blocks when a visual structure helps.
{% if image_generation_available %}
- Actively assess whether a new purpose-built educational illustration would teach better than text or Mermaid. Call `sfumato_image_gen` when it would, even if reusable artifacts are available; existing artifacts are options, not an image budget or restriction. Give it a precise subject, composition, labels, and learning purpose. Embed every returned `markdown_path` with Marp's `![height:420px](images/file.png)` syntax so it fits within the slide. Sfumato applies the project theme automatically.
{% else %}
- No image generation model is configured. Use Markdown and Mermaid for visuals.
{% endif %}
{% if reusable_assets %}
- The project provides these theme-compatible visual assets. They are copied only when the final deck references them. Use one only when it improves the deck, and embed its exact path with Marp image syntax:
{% for asset in reusable_assets %}
  - **{{ asset.name }}** — {{ asset.description }}; path=`{{ asset.reference }}`; theme=`{{ asset.theme }}`; type=`{{ asset.media_type }}`; alt=`{{ asset.alt_text }}`; tags=`{{ asset.tags | join(", ") }}`
{% endfor %}
{% endif %}
- Keep Mermaid diagrams simple: at most 6 nodes, short quoted labels, and no formulas inside labels.
- Put math formulas in normal Markdown outside Mermaid diagrams.
- Use `<br/>` inside Mermaid labels when a label needs two short lines.
- Do not use raw HTML, inline SVG, or HTML wrapper tags.
- Include a short learning objective slide.
- Explain the source material for a student.
- Use examples from the provided files.
- Add presenter notes with short teaching cues when useful.
- You may call Sfumato filesystem tools to list allowed directories or read allowed text files when more context is needed.
- When calling filesystem tools, prefer absolute paths under the allowed filesystem root.
- Explore selectively. After reading the most relevant files, stop calling tools and produce the final deck.

Source material:
{{ source_bundle }}
