Corrective retry:
Your previous version of this scene was rejected. The renderer measured it and reported every fault below, with the exact pixel amount where it applies. Those numbers are measurements of your own markup, not estimates: treat them as the amount you have to recover.

Faults to fix:
{{ retry_error }}

Your previous version of this scene:
{{ retry_invalid_response }}

Fix **every** fault listed, and change nothing else. Keep the composition, the timing, the animation and the copy that were not named — they already passed. A redesign spends the attempt and usually introduces new faults, so make the smallest edit that clears each measurement, and give yourself margin beyond it rather than landing exactly on the limit.
