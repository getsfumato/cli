Author a complete offline Hyperframes project. Return only JSON shaped as {"files":{"meta.json":"...","index.html":"...","compositions/scene-1.html":"..."}}. `index.html` must be a complete standards-mode document with `<!DOCTYPE html>`, `<html>`, `<head><meta charset="UTF-8"></head>`, and `<body>`. Inside `<body>`, the entry composition must be a root `<div>` with `id`, `data-composition-id`, `data-start`, `data-width`, and `data-height`; do not put composition metadata on `<html>`. For multi-scene videos author one reusable sub-composition per beat under `compositions/` and assemble it from `index.html`. Hyperframes resolves every asset path from the project root, including inside sub-compositions: use `assets/...`, `vendor/...`, or `compositions/...` and never `../assets/...` or any other `../` path. Load GSAP only from `./vendor/gsap.min.js` in the entry composition, create one finite paused timeline per composition, and register it on `window.__timelines` under the exact composition ID. Animate only seek-safe transforms (`x`, `y`, `scale`, `rotation`) and `opacity`; never tween layout or text-reflow properties such as `letterSpacing`, width, height, top, left, padding, or font size. Never assign CSS `transform` to an element that GSAP animates with x/y/scale/rotation; express its complete initial transform in `gsap.set(...)` or the `from` side of `tl.fromTo(...)`. Put initial hidden states in CSS or immediate `gsap.set(...)` calls outside the paused timeline, not `tl.set(...)` at time zero. Every timed visible element must have a unique `id`, `class="clip"`, `data-start`, `data-duration`, and `data-track-index`. Use no remote URLs, network calls, modules, narration, unseeded randomness, or filesystem paths outside `assets/`, `vendor/`, and `compositions/`.
