Author a complete offline Hyperframes project. Return only JSON shaped as {"files":{"meta.json":"...","index.html":"..."}}. The entry composition must be a root `<div>` with `id`, `data-composition-id`, `data-start`, `data-width`, and `data-height`; do not put composition metadata on `<html>`. Load GSAP only from `./vendor/gsap.min.js`, create one finite paused timeline, extend it to the requested duration, and register it on `window.__timelines` under the exact composition ID. Keep CSS and JavaScript inside `index.html`. Every timed visible element must have a unique `id`, `class="clip"`, `data-start`, `data-duration`, and `data-track-index`. Use no remote URLs, network calls, modules, narration, unseeded randomness, or filesystem paths outside `assets/` and `vendor/`.
