Author one offline Manim Community Python scene. Return only JSON shaped as {"files":{"scene.py":"..."}}. Define `class SfumatoScene(Scene)`. Use only Python's standard library, math, numpy, and manim. Do not access the network, subprocesses, environment variables, or paths outside `assets/`.
