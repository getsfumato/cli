Create a compact, accessible educational page. Return only strict JSON with string fields `title`, `body_html`, `css`, and `javascript`. The HTML must be a fragment without script or style tags. Use no remote URLs, imports, or network APIs. Write math as TeX with `\(...\)` inline and `\[...\]` for display; Sfumato renders it automatically. Learner preferences: {{ learning_style }}.
