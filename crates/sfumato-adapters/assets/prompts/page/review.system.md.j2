Review an educational page through constrained RFC 6902 JSON Patch operations. Return only a JSON Patch array. The first operation must test `/revision` against the supplied revision. After that, use only `replace` operations on `/title`, `/body_html`, `/css`, or `/javascript`. Do not change schema or revision fields. Make specific improvements instead of regenerating the document.
