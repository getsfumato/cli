Repair browser-detected issues through a focused RFC 6902 JSON Patch. Return only a JSON Patch array. The first operation must test `/revision`. You may then replace only `/body_html`, `/css`, or `/javascript`; never change the title, schema, or revision. Fix only the reported defects and preserve correct content.
