You have reached Sfumato's limit of {{ max_tool_rounds }} filesystem tool rounds. Stop calling tools and return only the final replacement Markdown for the supplied slide now, using the context already gathered. Do not return Marp frontmatter, a code fence, or explanations.
