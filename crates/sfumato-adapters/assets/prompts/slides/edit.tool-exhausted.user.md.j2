You have reached Sfumato's limit of {{ max_tool_rounds }} filesystem tool rounds. Stop calling tools and return the final conservative RFC 6902 JSON Patch now, using the context already gathered. Replace only existing `/slides/<id>/markdown` content and do not return Markdown or explanations.
